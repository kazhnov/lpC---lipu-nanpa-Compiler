#include "stdio.h"
#include "string.h"

int o = 5;
float i = 0.3f;
const bool p = false;
const string ln = "omekalike";

int main(int argc, string argv[]) {
      if (argc > 0) {
            printf("You have entered a letter %s\n", argv[1]);
      } else {
            fprintf(stderr, "You have made an error!\n");
            exit(1);
      }

      return 0;
}
